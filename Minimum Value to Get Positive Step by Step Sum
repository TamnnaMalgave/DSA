class Solution {
    public int minStartValue(int[] nums) {
        
        int max=0,sum=0;
        for(int i=0;i<nums.length;i++)
        {
            sum+=nums[i];
            max=Math.min(max,sum);
        }
        return 1-max;
    }
}
